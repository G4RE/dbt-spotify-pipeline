name: 'spotify_pipeline'
version: '1.0.0'
config-version: 2

profile: 'spotify_pipeline'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
analysis-paths: ["analyses"]

models:
  spotify_pipeline:
    staging:
      +materialized: view

    intermediate:
      +materialized: table

    marts:
      +materialized: table
